public class PointSystem
{
   private int userPoints;
   private bool userAnswer;
   

    public void addPoints() {
        
        int userPoints = 0;

        if (userAnswer == true) {
            userPoints = userPoints++;
        }
    }
    
    // when the user answers a question correctly, the method should give the user x amount of points per correct answer
    // if the user answers a question incorrectly it wont add any points
    
    // if users gets answers correctly
    // add 1 point
    // if users gets answer wrong
    //else add no points
    
}